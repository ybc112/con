# CON 质押项目 · 后台管理使用教程

> 适用：BSC 主网质押合约 `0x0B3943E0851341164D859DB56B6502c786EC8000`
> 后台入口：https://con-beige.vercel.app → 页头「管理」
> 更新：2026-09-14

---

## 一、谁能进后台

合约有两级权限，后台页面按钱包自动判断并显示对应徽章：

| 角色 | 判定方式 | 能做什么 | 不能做什么 |
|---|---|---|---|
| **Owner（全部权限）** | 钱包 == 合约 owner | 全部功能 | — |
| **管理员 / operator（运营权限）** | `operators(钱包) == true` | 开期、注资、结算、充值邀请储备、暂停/恢复 | 改参数、增删管理员、转移所有权、锁定参数 |
| **只读（无管理权限）** | 其他钱包 | 无 | 全部操作按钮禁用 |

> 当前 owner：`0x39bB78BAdEC9d906CA77aF6b0882D0114263544F`
> 当前管理员：`0x500F34Dd13c01E5277E0A2AF933eA83BDE51BC88`

---

## 二、进入后台

1. 打开 https://con-beige.vercel.app
   - 首次访问或页面有更新时，按 `Ctrl + Shift + R`（Mac 为 `Cmd + Shift + R`）**硬刷新**，避免浏览器缓存旧版本
2. MetaMask 切到 **BNB Smart Chain 主网**（链 ID 56）
3. 连接你的管理钱包
4. 页头出现「**管理**」入口 → 点击进入
5. 右上角徽章确认身份：应显示 **Owner（全部权限）** 或 **管理员（运营权限）**

> 如果看不到「管理」入口，说明当前钱包既不是 owner 也不是管理员。

---

## 三、页面结构总览

从上到下共 9 个区块：

| 顺序 | 区块 | 作用 |
|---|---|---|
| 1 | 系统诊断 | 自动体检，发现异常时用红/黄条提示 |
| 2 | 合约总览 | 余额、质押量、可分配储备、节点数、当前期 |
| 3 | 邀请储备充值 | 补充邀请奖励储备（**最常用的运营操作**） |
| 4 | 结算周期运营 | 开期 / 注资 / 结算 + 阶段倒计时 |
| 5 | 合约参数设置 | 邀请奖励、门槛、汇率、价格源、交互费 |
| 6 | 管理员（operator）管理 | 增删管理员（仅 owner） |
| 7 | 所有权转移 | 两步式移交合约所有权（仅 owner） |
| 8 | 风控与资金救援 | 提取误转入的代币 / BNB（仅 owner） |
| 9 | 危险操作 | 暂停开关、锁定全部参数（**不可逆**） |

页面顶部还有「刷新数据」按钮，链上数据每 30 秒自动刷新一次；交易成功后会自动刷新。

---

## 四、各区块详解

### 4.1 系统诊断

进页面先看这里。会自动检测以下情况：

| 提示 | 含义 | 处理 |
|---|---|---|
| 🔴 暂停 + 参数锁定 | **严重**：暂停将永久无法解除，用户本金锁死 | 立刻「恢复运行」，再考虑锁定 |
| 🟡 合约处于暂停状态 | 用户无法质押与提取本金 | 排查原因后点「恢复运行」 |
| 🟡 可分配储备偏低 | 储备已不足一次邀请奖励，新用户质押即将开始失败 | 立即去「邀请储备充值」 |
| 🟡 尚未开期 | 排名分红为 0，用户看不到分红收益 | 见 §5.2 开期流程 |
| 🔵 节点不足 10 个 | 现在开期会被标记「合并」顺延 | 等节点够 10 个再开 |
| 🔵 参数已锁定 | 安全模式，owner 仅保留运营能力 | 正常，无需处理 |

全部正常时显示「未发现异常」。

### 4.2 合约总览

六个统计卡：

| 指标 | 说明 |
|---|---|
| **合约 CON 余额** | 合约持有的全部 CON，**包含用户质押本金**，不等于可用储备 |
| **用户质押总量** | 链上 `totalStaked`，即欠用户的质押本金 |
| **可分配储备（估算）** | 合约余额 − 质押本金。含邀请储备 + 排名奖池。**低于邀请奖励单价时会变黄** |
| **排行榜节点数** | 当前有分数的节点数量 |
| **当前期号** | 附带本期奖池金额 |
| **累计已分配** | 历史累计发出的排名分红 + 邀请奖励 |

> ⚠️ 「可分配储备」是**估算值**。合约里 `inviteRewardPool` 是 internal 变量，没有公开读取接口，只能用「余额 − 质押本金」近似。实际可用的邀请储备会略低于此值。

### 4.3 邀请储备充值 ⭐

**这是日常运营最高频的操作。**

合约规则：每成功推荐 1 个达标用户（被推荐人质押 ≥ 100U 价值），合约扣 **100 CON** 作为邀请奖励锁定给推荐人。

**储备耗尽会怎样**：`_qualifyReferral` 会 `revert NoInviteReserve()`，导致**被推荐的新用户质押整笔回滚**——用户付了 gas 却质押失败。这是上线期最高频的故障。

页面显示：
- **按当前单价，可再支撑 N 人** —— 用可分配储备 ÷ 邀请奖励单价估算
- **当前授权额度** —— 你把 CON 授权给质押合约的额度

操作步骤：

1. 在输入框填写要充值的 CON 数量（建议一次充够，例如 100000）
2. 点「**授权（无限额度）**」→ 钱包确认
   - 只需授权一次，之后长期有效
   - 使用无限额度是为了**避免覆盖**用户在质押页设置的 `MaxUint256` 授权
3. 点「**充值邀请储备**」→ 钱包确认
4. 成功后页面会自动刷新，`可再支撑 N 人` 数字随之上升

> 注意：**必须走这个入口**。直接把 CON 转账到合约地址是**无效的**——合约不知道这笔币可以用于邀请奖励，`inviteRewardPool` 仍是 0，用户质押照样失败。而且 CON 是质押币，合约的 `recoverWrongToken` 拒绝回收，转错就永久卡死。

### 4.4 结算周期运营

**四个状态卡**：当前阶段 / 奖池金额 / 快照节点数 / 已领取

**阶段说明**：

| 阶段 | 含义 | 可执行操作 |
|---|---|---|
| 尚未开期 | 还没开过任何一期 | 开期 |
| 展示期 | 开期后 3 天内，**不可领取** | 注资 |
| 领取期 | 展示期后 7 天内，用户可领取分红 | 无（注资已锁定） |
| 领取期已结束，待结算 | 已过 10 天窗口，未领部分待结转 | 注资、结算 |
| 已结算 | 该期已结算 | 开新一期 |
| 节点不足已合并 | 快照节点 < 10 个，奖池顺延 | 结算 |

**时间轴**：显示领取开始 / 领取结束时间及倒计时（展示期显示距领取开始的倒计时）。

**三个按钮**：

| 按钮 | 何时可点 | 说明 |
|---|---|---|
| **开期（快照当前榜单）** | 上一期已结算、或尚未开期 | 瞬间锁定当期节点名单和分数。节点 < 10 个会被标记为「合并」，奖池顺延 |
| **结算当前期** | 领取期已结束，或合并期 | 未领完的部分自动滚入下一期，不流失 |
| **确认注资** | 展示期 / 合并期 / 待结算 | 向本期奖池注入 CON，金额由你决定 |

> 注资前同样需要「授权（无限额度）」，且**领取期内无法注资**（合约会 revert `ClaimWindowStarted`），页面会自动禁用按钮。

### 4.5 合约参数设置

**左栏（仅 owner 可改，参数锁定后全部禁用）**：

| 参数 | 当前值 | 说明 |
|---|---|---|
| 邀请奖励单价 | 100 CON | 每推荐一个达标用户发放的数量 |
| 邀请达标门槛 | 100 U | 被推荐人质押价值达到此值才触发奖励 |
| 固定汇率 | 1 | 1 CON = N U。未配置价格源时用它计分 |
| 价格源地址 | — | 设为 `0x0000...0000` 表示回退用固定汇率；填了则按链上实时价格计分 |

> 价格源当前值**读不到**——合约里 `priceFeed` 是 internal 变量，没有公开 getter。修改时请自行记录。

**右栏（交互费配置）**：

| 项 | 说明 |
|---|---|
| 手续费币种 | 留空 = 用 BNB（原生币）支付 |
| 每笔交互费 | 例如 `0.00065118`（BNB） |
| 收费地址 | 收取交互费的钱包 |
| 保存 | 三项需一起填写 |

限制：**手续费币不能设为质押币或奖励币**（防吸血），费用上限 **1 BNB**。

### 4.6 管理员（operator）管理

> 仅 owner 可操作。

1. 输入要授权/移除的钱包地址
2. 点「**授权为管理员**」或「**移除管理员**」
3. 钱包确认

管理员可执行：开期、注资、结算、充值邀请储备、暂停/恢复。

> 会获得 `pause()` 权限——请只授权给你完全掌控的钱包。

### 4.7 所有权转移

**两步式**，填错地址不会立即丢权限：

1. 当前 owner 输入**新 owner 地址** → 点「**发起转移**」→ 确认
2. 用**新地址钱包**连接后台 → 点「**接受所有权**」→ 完成交接

第 2 步完成前，原 owner 仍是 owner。转移后原 owner 立即失去全部权限。

> 用途：私钥轮换。一旦怀疑 owner 私钥泄露，立刻新建钱包并执行本流程。

### 4.8 风控与资金救援

> 仅 owner 可操作。

| 功能 | 说明 |
|---|---|
| **提取误转入的代币** | 填代币地址 + 接收地址 + 数量（**代币最小单位的整数**，如 1 个 18 位代币填 `1000000000000000000`）。质押币 / 奖励币 / 手续费币**不可提取** |
| **提取合约内误转入的 BNB** | 交互费用 BNB 支付时是即时转给收费地址的，合约正常不留存 BNB，此处仅用于救回误转入资金 |

### 4.9 危险操作

**暂停合约**（owner 和管理员都可操作）：

- 暂停后：质押、提取本金、开期**全部不可用**
- 页面「暂停」按钮在已暂停时自动禁用，「恢复运行」在未暂停时自动禁用

**锁定全部高危参数（不可逆）**：

- 锁死后**永久不可修改**：交互费、价格源、暂停开关、管理员、所有权
- 仅保留开期 / 注资 / 结算 / 充值邀请储备的运营能力
- 用途：向社区证明项目方无法 Rug

> 🔴 **暂停状态下此按钮会被强制禁用**。原因见 §8.1，这是硬性护栏，不要绕过。

---

## 五、日常运营流程

### 5.1 首次上线检查清单

- [ ] 合约 CON 余额充足（≥ 预期发放的邀请奖励总额）
- [ ] 「可分配储备」远大于 0，至少能支撑数百人
- [ ] 邀请奖励单价、达标门槛、汇率与白皮书一致
- [ ] 交互费配置正确，收费地址是正式钱包
- [ ] 系统诊断「未发现异常」
- [ ] 管理员列表正确

### 5.2 每期运营流程（10 天为一个完整周期）

```
第 0 天          第 3 天           第 10 天
 开期 ──────────► 展示期结束 ──────► 领取期结束 ──────► 结算 ──► 开下一期
（快照榜单）      （进入领取期）      （未领的结转）      （奖池滚入下期）
    └─ 注资窗口：开期后到领取期开始前 ─┘
```

1. **第 0 天：开期**
   - 确认「排行榜节点数」≥ 10（不足会自动合并顺延，这期白开）
   - 点「开期（快照当前榜单）」
   - 开期瞬间锁定当期名单和分数，之后用户新质押/提取**不影响**本期排名

2. **第 0 天：注资**
   - 填金额 → 「确认注资」
   - **必须在领取期开始前完成**（即开期后 3 天内）
   - 金额由你决定，未领完的部分会自动滚入下一期，不会流失

3. **第 3–10 天：领取期**
   - 用户在此期间领取分红
   - 你无需操作，可随时观察「已领取」数字

4. **第 10 天后：结算**
   - 点「结算当前期」
   - 未领部分写入 `pendingCarryover`，下一期开期时自动带入奖池

5. 回到第 1 步开下一期

> **建议**：固定一个节奏（例如每 15 天一期），提前在社群公告开期与注资时间。

### 5.3 补充邀请储备

- 定期查看「可分配储备」与「可再支撑 N 人」
- 当支撑人数接近 0，或系统诊断出现黄色「储备偏低」提示时立即充值
- 充值走 §4.3 流程

### 5.4 紧急情况

| 情况 | 处理 |
|---|---|
| 发现异常交易 / 疑似攻击 | 点「暂停」→ 排查 → 「恢复运行」 |
| 储备耗尽、大量用户质押失败 | 立即充值邀请储备（§4.3） |
| owner 私钥疑似泄露 | 立刻新建钱包 → 走 §4.7 所有权转移 → 转移原钱包资产 |
| 误把 CON 转到合约 | **无法取回**（CON 是质押币，被合约禁止提取） |

---

## 六、周期机制速查

| 项 | 值 |
|---|---|
| 展示期 | 开期后 3 天（不可领取） |
| 领取期 | 展示期后 7 天 |
| 完整周期 | 10 天 |
| 最少开期节点数 | 10 个（不足则本期合并顺延） |
| 质押锁仓期 | 15 天 |
| 单地址最多活跃质押 | 50 笔 |
| 邀请奖励 | 100 CON / 达标用户 |
| 邀请达标门槛 | 100 U |
| 交互费 | 0.00065118 BNB / 笔 |

**排名分红档位**（按开期时的快照节点数动态决定）：

| 节点数 | 分配方式 |
|---|---|
| ≤ 10 | 前 10 名平分 100% |
| 11–50 | 前 10 名 50%，11–50 名 50% |
| 51–100 | 前 10 名 50%，11–50 名 30%，51–100 名 20% |
| > 100 | 前 10 名 50%，11–50 名 30%，51–100 名 15%，101 名后共 5% |

同一档内平分，每档最后一名兜底尾差，保证奖池 100% 分完。

---

## 七、错误提示对照表

后台已接入合约自定义错误解码，报错会显示中文原因。常见如下：

| 提示 | 原因 | 怎么办 |
|---|---|---|
| 邀请奖励储备不足… | `NoInviteReserve` | 去「邀请储备充值」补储备 |
| 高危参数已锁定 | `ParamsLocked` | 参数已不可改，属正常 |
| 上一期尚未结算，请先结算再开新期 | `PreviousEpochNotSettled` | 先点「结算当前期」 |
| 当前没有进行中的期，请先开期 | `NoActiveEpoch` | 先开期再注资 |
| 该期已结算，无法再领取或注资 | `EpochAlreadySettled` | 开下一期 |
| 领取期尚未结束，不能结算 | `ClaimPeriodNotEnded` | 等到第 10 天后再结算 |
| 领取期已开始，本期注资金额已锁定 | `ClaimWindowStarted` | 本期无法再注资，下期提前 |
| 该期已经开过，不能重复开期 | `EpochAlreadyOpened` | 正常，勿重复点击 |
| 未收到代币，请检查余额与授权额度 | `NoTokensReceived` | 检查钱包余额与授权 |
| 当前钱包不是合约 owner… | `NotOwner` | 换 owner 钱包 |
| 当前钱包不是 owner 或管理员… | `NotAdmin` | 换有权限的钱包 |
| 合约已暂停 | `ContractPaused` | 先恢复运行 |
| 代币划转失败，请检查余额与授权额度 | `TransferFromFailed` | 重新授权 |
| BNB 交互费不足 | `InsufficientBnbFee` | 钱包留足 BNB |
| 不能提取质押代币 / 奖励代币 | `CannotRecover…` | 该代币被合约禁止提取 |

---

## 八、高危操作与注意事项

### 8.1 🔴 永远不要在暂停状态下锁定参数

这是**最容易造成永久事故**的操作组合。

合约源码：

```solidity
function unpause() external onlyAdmin {
    if (adminParamsLocked) revert ParamsLocked();   // 锁死后无法解除暂停
    paused = false;
    ...
}
```

而 `withdraw()` 带有 `whenNotPaused` 限制。因此一旦「**已暂停** + **已锁定参数**」同时成立：

- 暂停**永久无法解除**（`unpause` 被 `ParamsLocked` 挡住）
- 用户**本金永久锁死**，没有任何后门可救
- 合约已部署上链，**无法修改**

**后台已加硬性护栏**：暂停状态下「立即锁定」按钮被禁用，并在危险区显示红色告警。**请勿通过其他渠道绕过此限制。**

### 8.2 其他注意事项

| 事项 | 说明 |
|---|---|
| 锁定参数不可逆 | 一步到位，锁定前请确认所有参数已调好 |
| 私钥安全 | **任何情况下不要把私钥发到聊天工具、文档、代码里**。若已泄露，唯一补救是走 §4.7 转移所有权并转移资产 |
| 注资有窗口 | 领取期开始后无法注资，务必在开期后 3 天内完成 |
| 不要直接转账给合约 | CON 转给合约不会计入任何额度，且**无法取回** |
| 管理员钱包留足 BNB | 开期需把全部节点写入存储，节点多时单笔 gas 较高 |
| 开期前确认节点数 | < 10 个会自动合并顺延，等于白开一期 |

---

## 九、常见问题 FAQ

**Q1：为什么「可分配储备」和实际邀请储备对不上？**
合约里 `inviteRewardPool` 是 internal 变量，没有公开 getter，页面只能用「合约余额 − 质押本金」估算。该值包含邀请储备与排名奖池两部分。

**Q2：充值了邀请储备，但「可再支撑 N 人」没变化？**
该数字基于估算，且会随用户质押/提取而波动。若充值后完全无变化，请核对交易是否成功、是否走的 `fundInvitePool` 入口。

**Q3：开期后能反悔吗？**
不能。开期瞬间已快照当期节点名单与分数，之后用户的新质押、提取都不会影响本期排名。

**Q4：奖池没发完会怎样？**
不会流失。结算时未领取的部分写入 `pendingCarryover`，下一期开期时自动带入奖池。

**Q5：为什么用户质押失败了？**
看 §七 对照表。上线期最常见的是 `NoInviteReserve`（储备耗尽），去 §4.3 补储备即可。

**Q6：参数锁定后还能开期注资吗？**
能。锁定的只是「高危参数修改权」，开期、注资、结算、充值邀请储备这些运营能力仍然保留。

**Q7：忘记 owner 私钥了怎么办？**
无法找回。合约所有权绑定地址，没有后门。所以务必妥善备份，并建议提前规划多签或专用钱包。

---

## 十、合约信息速查

| 项目 | 值 |
|---|---|
| 质押合约 | `0x0B3943E0851341164D859DB56B6502c786EC8000` |
| CON 代币 | `0x66A585556138EbBb44Da0fF1324C796C44eb5ED1` |
| Owner | `0x39bB78BAdEC9d906CA77aF6b0882D0114263544F` |
| 管理员（operator） | `0x500F34Dd13c01E5277E0A2AF933eA83BDE51BC88` |
| 后台页面 | https://con-beige.vercel.app |
| BscScan | https://bscscan.com/address/0x0B3943E0851341164D859DB56B6502c786EC8000#code |
| 合约源码验证 | 已验证，源码与仓库 `contracts/NBTStakingBankV3.sol` 逐字节一致 |
